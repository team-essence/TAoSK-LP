import React, { FC } from "react";
import { GlobalStyle } from "styles/global/globalStyle";

const App: FC = () => {
  return (
    <div>
      <GlobalStyle />
    </div>
  );
};

export default App;
